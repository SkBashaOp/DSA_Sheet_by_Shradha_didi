class Solution:
    def simplifiedFractions(self, n: int) -> List[str]:
        result=[]
        seen_fractions=set()
        for i in range(1,n):
            for j in range(i+1,n+1):
                fraction=i/j
                if fraction not in seen_fractions:
                    result.append(f"{i}/{j}")  
                    seen_fractions.add(fraction)
        return result                  
