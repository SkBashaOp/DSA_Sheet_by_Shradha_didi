class Solution:
    def maxProfit(self, prices: List[int]) -> int:
        max=0
        n=len(prices)
        for i in range(n-1):
            if prices[i]<prices[i+1]:
                max+=prices[i+1]-prices[i]
        return max        
