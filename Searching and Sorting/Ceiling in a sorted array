#User function Template for python3
class Solution:
    def findCeil(self, arr, x):
        i=0
        n=len(arr)
        arr.sort()
        # if x <= arr[0]:
        #     return 0
        for i in range(n):
            if arr[i] >= x:
                return i
            # if arr[i] < x and arr[i + 1] >= x:
            #     return i + 1    
        return -1    
