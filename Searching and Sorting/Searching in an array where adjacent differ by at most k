#User function Template for python3

class Solution:
    def findStepKeyIndex(self, arr, k, x):
        n=len(arr)
        i=0
        while i<n:
            if arr[i] == x:
                return i
            step = max(1, abs(arr[i] - x) // k)
            i += step
        return -1        
